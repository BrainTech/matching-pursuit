To compile fftw mp5 do:
1. Go to lib/fftw-3.1.2 (cd lib/fftw-3.2.2)
2. Print

make clean
./configure --with-our-malloc16 --disable-shared --enable-static --disable-threads --enable-sse2
print make
go for coffe :-)

2. Go to main directory (cd ../..)

3. Print :
rm src/.depands
touch src/.depands
make clean
make dep
make

4. Have a fun with mp5.
